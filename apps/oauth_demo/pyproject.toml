[project]
name = "oauth-demo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "quart>=0.19",
    "hypercorn>=0.17",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/oauth_demo"]
